services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    restart: unless-stopped
  
  frontend:
    build: ./frontend  # ← Certifique-se que existe um Dockerfile aqui!
    ports:
      - "80:80"
    depends_on:
      - backend